services:
  postgres:
    image: postgres:16

    container_name: devora_postgres

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: MyDatabase@2005
      POSTGRES_DB: devora

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: